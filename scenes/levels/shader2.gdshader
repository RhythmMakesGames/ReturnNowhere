shader_type canvas_item;

//render_mode blend_add;

void vertex(){
	//if (VERTEX.x > 140.0) {COLOR = vec4(1.0, 0.0, 1.0, 1.0); }
}

void fragment() {
	// Called for every pixel the material is visible on.
}
